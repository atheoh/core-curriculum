console.log([1, 2, 3] + [4, 5]); 
/* The code above will output the string "1, 2, 34, 5" because the + operator 
first converts the two arrays into strings and then concatenates them into a 
single string.
*/